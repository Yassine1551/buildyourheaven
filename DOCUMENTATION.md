# توثيق مشروع "ابنِ جنتك" (Build Your Heaven)

> تطبيق أذكار إسلامي تفاعلي مع نظام تحفيز (Gamification) يهدف إلى تشجيع المستخدم على المداومة على ذكر الله، مدمج مع نظام مكافآت افتراضية يبني "جنة" المستخدم ذكراً ذكراً، إضافة إلى مكتبة آيات قرآنية للحفظ والتسميع.

---

## 1. الفكرة العامة

تطبيق موبايل (React Native / Expo) يقدم:

- **الأذكار اليومية**: أذكار الصباح، المساء، النوم، الاستيقاظ — بنصوص عربية كاملة وأدلة شرعية.
- **شجرة البطاقات**: سلسلة بطاقات ذِكر تُفتح تباعاً، كل بطاقة تُفتح بعد إنجاز متطلبات سابقتها.
- **الورد الشخصي**: ورد أذكار يومي قابل للتخصيص (عدد المنشود، الحفظ).
- **آيات للحفظ**: مكتبة من الآيات القرآنية (مرتبة مصحفياً) للتسميع، بأزرار تنقل بين البطاقات.
- **النظام التحفيزي**: كل نقرة = +1 استقامة، مستوى كل 50 استقامة، حسنات، ختمات، قصور، كنوز، رقاب، نخل.
- **لعبة العمر الافتراضي**: كل ذكر يضيف دقائق إلى "عمرك الافتراضي"، مع شريط تقدم نحو هدف (60 سنة).
- **الحديقة الجبّية**: عناصر لعب بصرية (نخيل، قصور، برك، تلال) تُبنى بتقدم المستخدم.

---

## 2. التقنيات المستخدمة

| التقنية | الإصدار/الغرض |
|---------|--------------|
| **React Native** | 0.81.5 |
| **Expo** | SDK 54 |
| **Expo Router** | التنقل بين الشاشات (Routing) |
| **TypeScript** | ~5.9.2 |
| **NativeWind** | 4.1.23 (Tailwind للـ RN) |
| **React Native Reanimated** | 4.1.1 (الأنيميشن) |
| **AsyncStorage** | التخزين المحلي |
| **Zustand** | 5.0.2 (إدارة الحالة) |
| **Redux + Redux Toolkit** | إدارة الحالة القديمة |
| **Supabase** | قاعدة البيانات الخلفية (قيد الإعداد) |
| **Apollo Client + GraphQL** | الاتصال بالـ API |
| **Expo Notifications** | الإشعارات المحلية |
| **Expo Haptics** | الاهتزاز التفاعلي |
| **Expo AV / Audio** | أصوات النقر |
| **expo-image** | تحميل وعرض الصور |
| **react-native-paper / MaterialIcons** | مكونات واجهة Material |
| **Lottie** | رسوم متحركة |
| **Shopify FlashList** | قوائم عالية الأداء |
| **react-native-svg** | الرسوم المتجهة (الحديقة، المحراب) |
| **react-native-view-shot** | مشاركة صور الأذكار |

---

## 3. هيكل المشروع

```
├── app/                         # شاشات Expo Router
│   ├── _layout.tsx              # الـ Root Layout
│   ├── index.tsx                # شاشة البداية (Splash) بشعار ذهبي متحرك
│   ├── +not-found.tsx           # صفحة 404
│   ├── (tabs)/                  # شاشات التبويب الرئيسية
│   │   ├── _layout.tsx          # Tab Navigator
│   │   ├── index.tsx            # الأذكار (الشاشة الرئيسية + الحديقة + الإحصائيات)
│   │   ├── quran.tsx            # القرآن الكريم
│   │   ├── badges.tsx           # الأوسمة
│   │   ├── rankings.tsx         # آيات للحفظ (تسميع)
│   │   └── notifications.tsx    # التنبيهات
│   ├── mihrab.tsx               # شاشة التسبيح (المحراب)
│   ├── morning-adhkar.tsx       # أذكار الصباح
│   ├── evening-adhkar.tsx       # أذكار المساء
│   ├── sleep-adhkar.tsx         # أذكار النوم
│   ├── wakeup-adhkar.tsx        # أذكار الاستيقاظ
│   ├── wird.tsx                 # الورد الشخصي
│   └── congratulations.tsx      # شاشة الاحتفال (مودال)
│
├── constants/
│   ├── theme.ts                 # الثيم المركزي (ألوان، ظلال، مسافات)
│   ├── Colors.ts                # ألوان الوضع الفاتح/الداكن
│   ├── config.ts                # إعدادات التطبيق (مفاتيح التخزين)
│   ├── verses.ts                # بيانات آيات الحفظ (VerseItem[])
│   ├── badges.ts                # تعريفات الأوسمة والمستويات
│   └── benefits.ts              # فضائل الأذكار
│
├── contexts/
│   └── AppContext.tsx            # السياق الرئيسي (State Management)
│
├── services/
│   ├── mockData.ts              # بيانات البطاقات (DhikrItem[]) والإحصائيات
│   ├── morningAdhkar.ts         # بيانات أذكار الصباح
│   ├── eveningAdhkar.ts         # بيانات أذكار المساء
│   ├── sleepAdhkar.ts           # بيانات أذكار النوم
│   ├── wakeupAdhkar.ts          # بيانات أذكار الاستيقاظ
│   ├── personalWird.ts          # الورد الشخصي
│   ├── rewardEngine.ts          # محرك حساب المكافآت (الحسنات، الإحصائيات)
│   ├── adhkarNotifications.ts   # جدولة الإشعارات حسب الوقت
│   ├── shareAdhkar.ts           # مشاركة الأذكار (نص + صورة)
│   └── soundService.ts          # تحميل وتشغيل الأصوات
│
├── components/
│   ├── DhikrPlayer.tsx          # عداد الذكر
│   ├── OnboardingModal.tsx      # مودال الترحيب
│   ├── WirdSettingsModal.tsx    # إعدادات الورد الشخصي
│   └── garden/                  # مكونات الحديقة
│       ├── FloatingIsland.tsx   # الجزيرة العائمة
│       ├── PalaceCitadel.tsx    # القلعة
│       ├── PalaceKeep.tsx       # القصر الرئيسي
│       ├── PalaceSVG.tsx        # رسم القصر
│       ├── PalmGrove.tsx        # بستان النخيل
│       ├── PalmIcon.tsx         # أيقونة النخلة
│       ├── PalmOasis.tsx        # الواحة
│       ├── RiverTerrain.tsx     # التضاريس النهرية
│       └── Terrain.tsx          # التضاريس الأساسية
│
├── template/                    # القالب الأساسي للتطبيق
│   ├── auth/                    # المصادقة (Supabase + Mock)
│   ├── core/                    # الإعدادات الأساسية (Config, Client)
│   └── ui/                      # مكونات UI عامة (Alert)
│
├── hooks/                       # Hooks عامة
│   ├── useColorScheme.ts
│   └── useThemeColor.ts
│
├── assets/
│   ├── images/                  # صور (bg-pattern, logo, mihrab-bg, ...)
│   └── fonts/                   # الخطوط (SpaceMono)
│
├── scripts/                     # أدوات بناء بيانات الآيات (add, reorder, sort...)
│   └── reset-project.js         # سكربت إعادة تعيين المشروع
│
├── app.json                     # إعدادات Expo
├── package.json                 # الاعتماديات
└── tsconfig.json                # إعدادات TypeScript
```

---

## 4. شاشات التطبيق

### 4.1 الشاشة الرئيسية `app/(tabs)/index.tsx`
- عرض بطاقات الأذكار (DhikrItem) مع حالة الفتح والقفل.
- الحديقة الجبّية: نخيل، قصور، برك، تلال (مكونات `garden/`).
- شريط التقدم للعمر الافتراضي (هدف 60 سنة).
- عرض الحسنات، المستوى، اللقب، الاستقامة.
- أسماء ذكورية قابلة للتخصيص (مثل «قانت الليل»، «مستغفر الأسحار»...).
- زر الورد الشخصي مع `WirdSettingsModal`.
- زر الدخول إلى الأذكار الموقوتة (حسب الوقت الحالي).

### 4.2 المحراب `mihrab.tsx`
- شاشة التسبيح الرئيسية لكل ذكر.
- عداد دائري، إحصائيات دقيقة، خاصية الضغط المطول لإعادة التعيين.
- عرض نص الذكر وفضله ودليله.
- رسوم متحركة (تمايل، دوران، تدرج ذهبي).

### 4.3 الأذكار الموقوتة
- **Morning Adhkar** — أذكار الصباح (توقيت 5:00–13:00).
- **Evening Adhkar** — أذكار المساء (14:00–20:00).
- **Sleep Adhkar** — أذكار النوم (21:00–0:00).
- **Wakeup Adhkar** — أذكار الاستيقاظ (1:00–4:00).
- كل شاشة تحتوي على `FlatList` مع تمرير سريع (swipe) بين الأذكار، وعداد دائري، ومودال للفضائل والأدلة.

### 4.4 الورد الشخصي `wird.tsx`
- ورد أذكار يومي قابل للتخصيص (إضافة/إزالة أذكار، عدد منشود يصل إلى 5000).
- عداد تقدم وورد اليوم، مع مكافآت.

### 4.5 شاشات التبويب الأخرى
- **القرآن** (`quran.tsx`) — قائمة سور مع معاينة للنص (بدون تبويب ظاهر، يفتح من مكان آخر).
- **الأوسمة** (`badges.tsx`) — أوسمة تصاعدية (برونزي، فضي، ذهبي) تُمنح عند الإنجازات.
- **آيات للحفظ** (`rankings.tsx`) — مكتبة آيات الحفظ مع مودال التسميع.
- **التنبيهات** (`notifications.tsx`) — جدولة إشعارات للأذكار حسب الوقت، إشعار اختباري، إعدادات محلية.

### 4.6 شاشة الاحتفال `congratulations.tsx`
- تظهر عند فتح بطاقة جديدة أو الوصول إلى milestone.
- تحتوي على رسوم متحركة (نجوم، توهج).

---

## 5. نظام بطاقات الأذكار (Dhikr Cards)

البيانات في `services/mockData.ts` — ترتيب فتح البطاقات متسلسل، كل بطاقة تفتح بعد إتمام متطلبات سابقتها.

### واجهة `DhikrItem`

| الحقل | الوصف |
|-------|-------|
| `id` | معرف فريد |
| `title` | اسم الذكر |
| `dhikrText` | نص الذكر |
| `fadl` | الفضل |
| `targetCount` | عدد مرات التكرار لتحقيق الهدف |
| `icon` / `color` | أيقونة ولون البطاقة |
| `hasanatPerCount` | الحسنات لكل نقرة |
| `daleel` / `source` | الدليل ورقم الحديث |
| `extraLifeMinutes` | دقائق إضافية للعمر الافتراضي |
| `slavesFreed` | الرقاب المعتوقة (حرز) |
| `isShield` | هل هو حرز |
| `purgeSins` | حط الخطايا |
| `sadaqah` | الصدقات |
| `levels` | الدرجات |
| `salat` | صلوات الله على المستخدم |
| `palms` | النخيل |
| `unlockRequirement` | شرط الفتح (نصي) |
| `unlockKey` / `unlockValue` | شرط الفتح المنطقي |
| `secondUnlockKey` / `secondUnlockValue` | شرط إضافي |
| `requireAllPrevious` | يتطلب إتمام كل ما قبله |
| `order` | ترتيب الفتح |

### سلسلة البطاقات الحالية (مثال من الكود)

```
مغفرة الذنوب  →  ألف حسنة  →  نخلة في الجنة  →  حط الخطايا
  →  حرز من الشيطان  →  الصلاة على النبي ﷺ
  →  ثلث القرآن  →  كنز الجنة  →  قصر في الجنة
  →  صدقات الأذكار  →  جوامع الكلم  →  جوهرة الأذكار
  →  ملء الميزان
```

---

## 6. نظام آيات الحفظ (Verses — `constants/verses.ts`)

> هذا هو **التبويب «آيات للحفظ»** في `app/(tabs)/rankings.tsx` — مكتبة آيات قرآنية للتسميع.

### واجهة `VerseItem`

| الحقل | الوصف |
|-------|-------|
| `id` | معرف فريد (سلسلة رقمية، `id === order`) |
| `title` | عنوان البطاقة (مثال: «قصة قارون (القصص 76-78)») |
| `verses` | نص الآيات مضمّن برموز الآيات (﴿﴾) |
| `virtue` | فضيلة/وصف البطاقة |
| `order` | ترتيب العرض (1..101 حالياً) |

### الخصائص

- **101 بطاقة** مرتبة مصحفياً (الفاتحة ← ... ← الانشقاق).
- تُفتح وتُسجّل كـ«محفوظة» عبر مفتاح `memorized_verses` في AsyncStorage.
- مودال التسميع (`FocusModal`) يتيح:
  - تنقلاً بين البطاقات (سابق/تالي).
  - تخصيص **حجم الخط** (12–36) و**التباعد بين الأسطر** — مع إعدادات افتراضية خاصة ببعض البطاقات المطوّلة (مثال: خط 16–17 وتباعد مخفَّض).
  - وضع علامة «محفوظة».
  - مشاركة البطاقة.
- السكربتات في `scripts/` (add-verses, reorder-verses, sort-by-mushaf, insert-saba...) تساعد في بناء هذه البيانات وترتيبها.

---

## 7. نظام المكافآت (Reward Engine)

الملف: `services/rewardEngine.ts`

### الصيغ

**للأذكار القرآنية:**
```
عدد الحروف = countArabicLetters(text)
الحسنات = عدد الحروف × 10 × target
```

**للأذكار غير القرآنية:**
```
الحسنات = hasanatPerCount × عدد النقرات (تحتسب فورياً في الـ Context)
```

### الإحصائيات المتزامنة (Sync Targets)

| الهدف العربي | المفتاح الداخلي | الوظيفة |
|-------------|----------------|---------|
| حرزك من الشيطان | `hirz_status` | يزيد بعدد الـ target |
| سيئاتك الممحية | `sayyiat` | يزيد بعدد الـ target |
| ختمة | `khatma` | يزيد بمقدار 1 |
| الأصول (Assets) | `treasures` | يزيد بمقدار 1 |

### الإحصائيات المخزنة (`stats`)

| المفتاح | الوصف |
|---------|-------|
| `palms` | عدد النخيل |
| `palaces` | القصور (كل 10 صلوات على النبي = قصر) |
| `khatma` | الختمات |
| `qusur` | القصور في الجنة (من سورة الإخلاص) |
| `salawat` | صلوات الله على المستخدم |
| `sadaqat` | الصدقات |
| `riqab` | الرقاب المعتوقة |
| `hirz_status` | مرات التحصن (حرز من الشيطان) |
| `level_points` | نقاط المستوى (الدرجات) |
| `treasures` | الكنوز |
| `elapsed_days` | الأيام المنقضية |
| `extra_life_minutes` | الدقائق المضافة للعمر الافتراضي |

### المستوى والاستقامة

```
استقامة = إجمالي عدد النقرات على جميع الأذكار
المستوى = floor(استقامة / 50) + 1
```

### الألقاب

| الحد الأدنى | اللقب |
|------------|-------|
| 0 | مبتدئ |
| 10 | صاحب |
| 30 | مواظب |
| 60 | ذاكر |
| 100 | مسبّح |
| 200 | عابد |
| 500 | قانت |

---

## 8. الثيم (Theme)

الملف: `constants/theme.ts`

- **الألوان الأساسية**: أخضر غامق (#064E3B) مع ذهبي (#D4AF37) — ألوان إسلامية فاخرة.
- **الخلفية**: أسود مخضر (#021A13).
- **الكروت**: شفافة بتأثير زجاجي (Glassmorphism).
- **الظلال**: تأثير توهج ذهبي (glow effect).
- **التدرجات**: ذهبية (goldGradient = ['#D4AF37', '#F0D060', '#D4AF37']).
- **الطباعة**: خطوط عريضة (800 weight) للأرقام والعناوين.

---

## 9. التخزين المحلي (AsyncStorage)

| المفتاح | المحتوى |
|---------|---------|
| `hasanat_total` | إجمالي الحسنات |
| `dhikr_counts` | عدد نقرات كل بطاقة |
| `stats_data` | الإحصائيات (ختمات، قصور، كنوز...) |
| `user_name` | اسم المستخدم |
| `show_welcome` | هل تظهر شاشة الترحيب |
| `user_level` | المستوى الحالي |
| `user_istiqama` | نقاط الاستقامة |
| `unlocked_cards` | البطاقات المفتوحة |
| `sound_enabled` | تفعيل الصوت |
| `vibration_enabled` | تفعيل الاهتزاز |
| `use_western_numerals` | الأرقام الغربية/الشرقية |
| `target_years` | سنوات الهدف (افتراضي: 60) |
| `morning_counts` | عدادات أذكار الصباح |
| `sleep_counts` | عدادات أذكار النوم |
| `evening_counts` | عدادات أذكار المساء |
| `wakeup_counts` | عدادات أذكار الاستيقاظ |
| `review_state` | حالة تقييم التطبيق |
| `memorized_verses` | آيات الحفظ المحفوظة |

---

## 10. الإشعارات (Notifications)

الملف: `app/(tabs)/notifications.tsx`

- إشعارات مجدولة لأوقات الأذكار الأربعة.
- المستخدم يختار الوقت المناسب لكل مجموعة.
- إرسال إشعار اختباري لتجربة الإعدادات.
- تخزين الإعدادات محلياً.
- ربط الإشعارات بالأذكار الموقوتة عبر `services/adhkarNotifications.ts`.

---

## 11. البنية المستقبلية (Roadmap)

- [ ] **قاعدة بيانات خلفية (Supabase)** — لمزامنة الإنجازات بين الأجهزة.
- [ ] **نظام المصادقة** — تسجيل الدخول بحساب واحد لمزامنة البيانات.
- [ ] **لوحة متصدرين حقيقية** — تصنيف عالمي للمستخدمين.
- [ ] **ميزة الدفع (Stripe)** — دعم تطوعي أو رفع الحظر عن ميزات.
- [ ] **الوضع الليلي/النهاري التلقائي** — بناءً على الوقت.
- [ ] **ترجمة التطبيق** — لغات متعددة (الإنجليزية، الفرنسية...).
- [ ] **إحصائيات متقدمة** — رسوم بيانية يومية/أسبوعية/شهرية.
- [ ] **خطط مخصصة** — المستخدم يحدد أهدافه اليومية.
- [ ] **Web Version** — تشغيل التطبيق عبر المتصفح.

---

## 12. كيفية التشغيل

### المتطلبات
- Node.js ≥ 18
- Expo CLI (`npm install -g expo-cli`)
- Expo Go App على الهاتف

### الخطوات

```bash
# 1. تثبيت الاعتماديات
npm install

# 2. تشغيل الخادم
npx expo start

# 3. فتح Expo Go على الهاتف ومسح QR code
```

### الأوامر المتاحة

| الأمر | الوظيفة |
|-------|---------|
| `npm start` | تشغيل خادم Expo |
| `npm run android` | تشغيل على Android Emulator |
| `npm run ios` | تشغيل على iOS Simulator |
| `npm run web` | تشغيل النسخة الويب |
| `npm run lint` | تشغيل ESLint |
| `npm run reset-project` | إعادة تعيين المشروع |

---

## 13. مراحل البناء

### المرحلة 1 — الأساسيات ✅ (مكتملة)
- [x] هيكل المشروع Expo + TypeScript
- [x] التنقل بين الشاشات (Expo Router)
- [x] الثيم المركزي (الألوان، الخطوط، الظلال)
- [x] التخزين المحلي (AsyncStorage)
- [x] الشاشة الرئيسية مع البطاقات

### المرحلة 2 — نظام الأذكار ✅ (مكتملة)
- [x] أذكار الصباح، المساء، النوم، الاستيقاظ بالنصوص والأدلة
- [x] شاشات تفاعلية مع عدادات دائرية وسحب جانبي
- [x] حساب المكافآت والحسنات

### المرحلة 3 — Gamification ✅ (مكتملة)
- [x] نظام البطاقات المقفلة
- [x] محرك المكافآت (Reward Engine)
- [x] المستويات والاستقامة والألقاب
- [x] العمر الافتراضي والهدف السنوي

### المرحلة 4 — اللمسات الجمالية ✅ (مكتملة)
- [x] الرسوم المتحركة (Reanimated)
- [x] الاهتزاز التفاعلي (Haptics)
- [x] صوت النقر
- [x] شاشة الاحتفال
- [x] المؤثرات البصرية (توهج، تدرجات، زجاجية)

### المرحلة 5 — الميزات الثانوية ✅ (مكتملة)
- [x] شاشة القرآن
- [x] نظام الأوسمة
- [x] **آيات للحفظ** (مكتبة آيات التسميع مع تخصيص الخط)
- [x] **الورد الشخصي** (ورد أذكار قابل للتخصيص)
- [x] الإشعارات المجدولة
- [x] التخصيص (الأرقام الشرقية/الغربية، الصوت، الاهتزاز)

### المرحلة 6 — القادمة 🔄
- [ ] Supabase Backend
- [ ] المصادقة (Auth)
- [ ] المزامنة السحابية
- [ ] تصنيف عالمي حقيقي
- [ ] الدفع (Stripe)
- [ ] توطين (i18n)

---

> آخر تحديث: 6 أغسطس 2026
